<configuration xmlns="http://www.jooq.org/xsd/jooq-codegen-3.4.0.xsd">

<!-- Configure the database connection here -->
<jdbc>
    <driver>com.mysql.jdbc.Driver</driver>
    <url>jdbc:mysql://db-master:3306/ehcore</url>
    <user>ehcore</user>
    <password>ehcore</password>
</jdbc>

<generator>
    <!-- The default code generator. You can override this one, to generate your own code style
    Defaults to org.jooq.util.DefaultGenerator -->
    <name>org.jooq.util.DefaultGenerator</name>
    <generate>
        <daos>true</daos>
    </generate>
    <database>
        <!-- The database type. The format here is:
        org.util.[database].[database]Database -->
        <name>org.jooq.util.mysql.MySQLDatabase</name>

        <!-- The database schema (or in the absence of schema support, in your RDBMS this
            can be the owner, user, database name) to be generated -->
        <inputSchema>ehcore</inputSchema>
        <!-- All elements that are generated from your schema
            (A Java regular expression. Use the pipe to separate several expressions)
            Watch out for case-sensitivity. Depending on your database, this might be important! -->
        <includes>
            .*
        </includes>

            <!-- All elements that are excluded from your schema
            (A Java regular expression. Use the pipe to separate several expressions).
            Excludes match before includes -->
        <excludes>
            eh_acls|eh_acl_privileges|eh_acl_roles|eh_acl_role_assignments|eh_apps|eh_app_profiles|eh_configurations|
            eh_content_shard_map|eh_messages|eh_message_boxs|eh_namespaces|eh_sequences|eh_servers|eh_server_shard_map|eh_shards
        </excludes>
    </database>
    <target>
        <!-- The destination package of your generated classes (within the destination directory) -->
        <packageName>com.everhomes.server.schema</packageName>
        <!-- The destination directory of your generated classes -->
        <!-- code generation will be run under the project root using maven, it is then relative to where POM file locates -->
        <directory>src/main/java</directory>
    </target>
</generator>

</configuration>
