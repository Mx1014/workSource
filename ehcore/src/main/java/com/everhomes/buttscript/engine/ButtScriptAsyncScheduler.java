package com.everhomes.buttscript.engine;

import com.everhomes.gogs.GogsRepo;

import java.util.HashMap;
import java.util.Map;

/**
 * 异步的脚本调用
 */
public class ButtScriptAsyncScheduler implements ButtScriptScheduler {

    @Override
    public void doButtScriptMethod() {
/*        GogsRepo anyRepo = gogsService.getAnyRepo(...);
        Map<String, Object> param = new HashMap<>(3);
        param.put("scriptPath", myScriptPath);// 脚本路径需要自己存储
        param.put("lastCommit", myScriptLastCommit);// 上次提交 id 也需要自己存储
        param.put("gogsRepo", anyRepo);
        scriptEngineService.push(new GogsScriptDummy(param));*/
    }
}
